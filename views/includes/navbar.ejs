<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <style>
    .navbar-brand {
      font-size: 1.5rem;
      font-weight: bold;
      color: #007bff;
      transition: color 0.3s ease;
    }

    .navbar-brand:hover {
      color: #0056b3;
    }

    .nav-link {
      transition: transform 0.3s ease, color 0.3s ease;
      position: relative;
      color: #007bff;
    }

    .nav-link::after {
      content: "";
      display: block;
      width: 100%;
      height: 2px;
      background: #007bff;
      transform: scaleX(0);
      transition: transform 0.3s ease;
      position: absolute;
      left: 0;
      bottom: -5px;
    }

    .nav-link:hover {
      color: #0056b3;
    }

    .nav-link:hover::after {
      transform: scaleX(1);
    }

    .btn-outline-success {
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .btn-outline-success:hover {
      background-color: #28a745;
      color: white;
    }

    .btn-outline-danger {
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .btn-outline-danger:hover {
      background-color: #dc3545;
      color: white;
    }

  </style>
</head>

<body>

  <body class="p-0 m-0 border-0">

    <nav class="navbar navbar-expand-lg bg-light p-3">
      <div class="container-fluid">
        <a class="navbar-brand" href="/blogs">BlogSphere</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/home">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="https://github.com/mic-720" target="_blank">About</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/blogs/new">Write new Blog</a>
            </li>
          </ul>

          <!-- User auth buttons -->
          <% if (!currUser) { %>
            <div class="d-flex flex-column flex-lg-row">
              <a href="/auth/signup" class="btn btn-outline-success mb-2 mb-lg-0 me-lg-2">Signup</a>
              <a href="/auth/signin" class="btn btn-outline-success">Signin</a>
            </div>
            <% } else { %>
              <ul class="navbar-nav mb-2 mb-lg-0 align-items-center">
                <li class="nav-item me-3">
                  <a class="nav-link disabled" aria-disabled="true">
                    <strong>Welcome, <%= currUser.username %>!</strong>
                  </a>
                </li>
                <li class="nav-item">
                  <a href="/auth/logout" class="btn btn-outline-danger">Logout</a>
                </li>
              </ul>
              <% } %>
        </div>
      </div>
    </nav>

    <style>
      /* Additional custom styles can go here */
    </style>
  </body>

</body>

</html>